import 'package:flutter/material.dart';

class WallScreen extends StatelessWidget {
  static const String id = '/wall_screen';

  WallScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
